import { fireEvent, render, screen } from "@testing-library/react"
import CourseList from "./index"

describe('pruebas en lista de cursos', () => {
    // const title = 'Lista de cursos';

    
    // test('prueba de que se muestre el titulo de lista de cursos', () => {
    //     render(<CourseList />)
    //     expect(screen.getByText(title)).toBeTruthy();

    // })

    // test('prueba de que se muestre el titulo dentro de h2', () => {
    //     render(<CourseList />)
    //     expect(screen.getByRole('heading', { level:2 }).innerHTML ).toContain(title);

    // })

    
    // test('prueba de que se se muestre 11 al dar click a +1', () => {
    //     render(<CourseList />)
    //     expect(screen.getByText('10')).toBeTruthy();

    //     fireEvent.click(screen.getByText('+1'));
    //     // fireEvent.click(screen.getByRole('button',  {name: }).innerHTML);
    //     expect(screen.getByText('11')).toBeTruthy();

    // })
    
    // test('prueba de que se se muestre 9 al dar click a -1', () => {
    //     render(<CourseList />)
    //     fireEvent.click(screen.getByText('-1'));
    //     expect(screen.getByText('9')).toBeTruthy();

    // })

    // test('debe funcionar el boton de reset', () => {
    //     render(<CourseList />)
    //     fireEvent.click(screen.getByText('-1'));
    //     fireEvent.click(screen.getByText('-1'));
    //     fireEvent.click(screen.getByText('-1'));
    //     expect(screen.getByText('13')).toBeTruthy();
    //     fireEvent.click(screen.getByText('reset'));
    //     expect(screen.getByText('10')).toBeTruthy();

    // })
    
})